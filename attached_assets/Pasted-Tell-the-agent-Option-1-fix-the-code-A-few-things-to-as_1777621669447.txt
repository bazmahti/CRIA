Tell the agent: Option 1 — fix the code.
A few things to ask the agent to confirm as it works, so we don't end up with a partial fix:
Schema. A research_jobs table with these columns: id (uuid, primary key), status (text: queued/running/complete/failed), created_at (timestamp), started_at (timestamp, nullable), completed_at (timestamp, nullable), result_json (jsonb, nullable), error_text (text, nullable), question_text (text), mode (text, nullable for now). Indexed on id and on created_at for the findings index page.
Two write points and one read point. When the POST /cria-unified/research handler creates a job: insert a row with status queued. When _run_research_job starts: update to running. When it completes: update to complete with result_json. When it fails: update to failed with error_text. The polling endpoint GET /cria-unified/research/{job_id} reads from the table instead of the in-memory dict.
Keep the in-memory dict OR remove it. Don't dual-write. Either fully replace _research_jobs with database calls, or keep it as an optional cache layer with the database as the source of truth. The simpler fix is to remove it entirely — that's the path I'd recommend. Half-replacement is where bugs hide.
Connection handling. The Python service should use a connection pool (likely asyncpg or psycopg[async] with a pool) — not a single connection — because multiple channels and the polling endpoint will be reading/writing concurrently. If the agent uses the existing PostgreSQL Drizzle connection from the Node side, that won't work — they're different language runtimes. The Python service needs its own connection to the same database.
Logging. While the agent is in there, ask it to add the logging we discussed earlier — log.info on job start/complete, log.error on failure with exc_info=True, and log.warning on polled-but-not-found in the GET endpoint.
Testing. After the change, two tests: (1) submit a query in dev mode, confirm it completes via dashboard (regression test); (2) submit a query against the deployed *.replit.app URL, confirm it completes (the actual fix verification). The second test is what's been failing all along.
Tell the agent to proceed, paste the agent's plan back here if you want me to sanity-check it before they write code. And let me know how it goes.